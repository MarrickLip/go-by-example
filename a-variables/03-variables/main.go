package main

import "fmt"

func main() {
	var a = "apple"

	// := is shortand for declaring an initialising
	b := "banana"

	// variables can be initialised with a type and an default value
	var i int // defaults to 0

	fmt.Println(a, b, i)
}
